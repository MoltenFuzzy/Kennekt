const config = {
  MAX_FILE_SIZE: 2000000, // 20 mb
  MAX_UPLOAD_FILES: 5,
  PRESIGNED_URL_EXPIRATION: 3600,
  ALLOWED_FILE_TYPES: ["image/png", "image/jpeg", "image/jpg"],
};

export default config;
